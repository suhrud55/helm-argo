apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-s.fullname" . }}
  labels:
    {{- include "project-s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project-s.selectorLabels" . | nindent 4 }}
